import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Dungeon Crawler Carl Book Companion',
  webDir: 'dist'
};

export default config;
